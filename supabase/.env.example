# Family Ops — Supabase project-level secrets template
# These are read by `supabase/config.toml` itself (env(...) references), not
# by Edge Functions — see supabase/functions/.env.example for those.
# Copy to supabase/.env.local for `supabase start` and never commit real values.

# Supabase Auth's own Google Sign-In provider (PWA login / onboarding).
# Distinct from the separate Google Calendar OAuth client used by
# google-calendar-oauth-start/-callback — see docs/design/v6/01_ARCHITECTURE.md
# and docs/design/v6/14_EXTERNAL_SETUP_STEPS.md #3 "App Google Sign-In".
GOOGLE_SIGNIN_CLIENT_ID=
GOOGLE_SIGNIN_CLIENT_SECRET=
GOOGLE_SIGNIN_REDIRECT_URI=http://localhost:5173/auth/callback
